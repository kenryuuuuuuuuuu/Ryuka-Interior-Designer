# 日常利用へのフォルダ統合記録

2026-09-11。施主承認に基づき実施。ゲストの編集・UE確認の反復を優先し、自宅開発は保留します。

## Gitとソース

- 元main：190a135。ゲスト受入HEAD：02a4f07。
- 未コミットだったW08-G最終レビュー、cmd起動修正、CRLF指定と日常案内/残課題を開発ブランチへ保存。
- mainが開発ブランチの祖先であることを確認し、4244bc9へfast-forward統合。既存mainの独自変更を上書きする競合はありませんでした。
- 旧worktreeをGitの正規手順で解除。mainだけが作業場所として登録されています。開発ブランチと過去コミットは履歴として残します。リモートへのpushはしていません。
- トップのHTMLとguest-launcher.cmdが日常入口です。AGENTS.md/CLAUDE.md/README/STATUS/試用ガイドを更新しました。

## ローカル成果物の移行

旧worktreeのbuildから、トップのbuildへ次を同名で移しました。コピーの競合・上書きはありません。

- launcher：設定、比較画像、取込前バックアップ、ログ
- scenarios：名前付き案
- W07-G3-refresh-v2、W08-G-update-v2、W08-G-update-v3：登録済みモデルと戻り先

現行モデルは `build/W08-G-update-v3/ue` のままです。相対パスの設定はそのまま使え、3モデルのrepo-root.jsonだけをトップへ接続し直しました。修正前ファイルはアーカイブに控えています。保存状態、案、画像の内容は変更していません。

残りの旧buildは `build/archive/2026-09-11-consolidation/generated-build/` へ、旧作業場所のGit管理外資料・キャッシュ等は同階層の `worktree-local/` へ保存しました。旧検証報告の絶対パス・ハッシュは当時の証跡として保持しています。アーカイブは日常の編集対象ではありません。

移行計画・ハッシュ・接続先の修正前ファイル・検証結果は上記アーカイブのmigration-plan.json、migration-result.json、final-checks.jsonに保存しました。機械固有パスを含むためGit除外です。

## 確認

- 移行前後の保護対象156ファイル：全件SHA-256一致。その確認後にrepo-root.jsonのみを明示的に修正。
- トップでunittest：221件成功。建物/家具/電気/開口validatorとWeb生成データの--check成功。
- トップのcmd入口経由でstatus：終了コード0、依存不足なし、現在モデルvalid=true、scope=guest。
- 保存案22件、壊れた案0。比較記録2件、参照画像の欠落0。
- 最新保存の共通選択が成功。更新用の正本参照エラー0。
- UEモデルの再生成や新たな手動walkthroughは行っていません。既存モデルを保存状態ごと移し、接続先とランチャーからの参照を検証しました。

## 残る注意

旧 `build/worktrees/visual-twin` の内容は除去済みですが、空ディレクトリだけが別プロセスの使用中エラーで残りました。Git worktreeではなく、HTML/cmd等の二重ソースもありません。利用しているウィンドウを閉じた後なら空フォルダを削除できます。強制的なプロセス終了はしていません。

ブラウザ内にだけ残る未書出しの家具配置はファイル移動の対象外です。旧タブに残っている場合はJSONを書き出してからトップの入口で取り込んでください。自宅側の未実装、洗濯機の向き、仮材質/未校正照明、狭所等は [BACKLOG.md](../BACKLOG.md) に引き継ぎました。
