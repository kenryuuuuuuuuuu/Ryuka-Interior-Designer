# W08-F 全館の日常利用・初期版引渡し

状態：初期実装・代表検証完了。施主の操作・画質評価は確認待ち。施主の2026-09-12指示でGPTが実装・検証します。
BASE: `fc4a6c3`、ブランチ: `codex/w08-full`。トップの作業場所で開発し、入れ子worktreeを追加しません。

## 範囲

既存ランチャーをguest/home/wholeへ拡張します。モデルの範囲を表示し、更新は選択元と同じscopeを保持します。既存の案と設定を移行せず継続使用します。全館では前回の続き、自宅入口、ゲスト入口を選べます。入口指定はセッションのカメラだけを変え、保存ファイルと室別条件を起動前に書き換えません。旧モデルは更新まで前回の続きが使用可能です。

日常入口は `interior-launcher.cmd` とし、旧 `guest-launcher.cmd` も互換入口として残します。家具編集を開く→JSON取込→モデル更新→内覧→案保存/比較記録の導線を既存処理へ接続します。処理が競合する同時実行を拒否します。

## 検証

scope別更新の単体テスト、設定往復、入口選択の有効範囲、実GUI構築と通常処理の接続を確認します。全館の実更新、2つの入口からの実UE起動、F5条件保持、案保存とA/B記録、共有コピーを代表確認します。ゲストのモデルと既存保存案を保持します。

## 引渡しの境界

実装者の検証と施主の画質・操作評価は区別します。未校正の露出・光束、階段照明の重複位置、未確定の寸法/採用品、狭所の制限はBACKLOGへ引き継ぎます。写真同等化、配布exe、VR・クラウド共同編集は含めません。
