# <ID> <成果>

状態：DRAFT / READY。推奨モデル：Sonnet 5 / Opus 5（理由）。

## 開始点

対象ブランチ/ワークツリー、前提コミット、依存タスク。実装開始時の完全なHEADをBASEとして固定し、未コミット変更がある場合は混ぜない。

## 目的と範囲

利用者の操作と得られる結果。今回含めないもの。

## 最小限の参照

AGENTS.md、初回BACKGROUND.md、DEVELOPMENT_WORKFLOW.mdに加え、今回必要な仕様とコードのパス。

## 実装契約

入出力の例、正本・単位・座標・永続ID・旧版互換、失敗時の挙動、守るべき不変条件。固定する設計判断と実装者に任せる判断を区別する。

## 手順

再現→原因/依存確認→実装→検証。想定する変更ファイルと、範囲外なら相談する変更。

## 受入条件

| ID | シナリオ・期待結果 | 必要な証拠 |
|---|---|---|
| AC1 | 正常系 | コマンドと結果 |
| AC2 | 異常系/互換性 | 再現テスト |
| AC3 | 既存機能の回帰 | 対象を絞ったチェック |

具体的な検証コマンド、実機で必要な操作、未実行時の扱いを明記する。

## 停止条件と提出物

仕様変更が必要な条件、調査上限、報告先。完了報告・レビュー束の場所。次のタスクは自動着手しない。
