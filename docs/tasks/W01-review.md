# W01 レビュー

判定：BLOCKED（描画復旧未完了）。報告には下記の必須修正があります。
対象BASE：9882f564587d4f40ec9721c82c095aa1458c7596
対象HEAD：b75480b6896e31c9cbfdd387ba34e1f30661e0a4
参照：build/reviews/W01-v1/manifest.json

## 確認範囲

Git差分は報告書1件のみで、アプリコード変更はありません。差分ハッシュと証拠7件のサイズ/SHA-256は全て一致しました。報告全体、主要ログ、実デバイス生成プローブを読みました。アプリ変更がないため既存48テストは再実行していません。AC2/3は未完了、AC4のテスト成功は実装者報告です。AC5は現段階で修正なしのN/Aとし、復旧後に必要性を再判断します。

## 必須修正

- R1：`d3d12_device_probe.log` はD3D12CreateDevice(default)の成功後、GetNodeCount呼出しでaccess violationが発生しています。スクリプト全体は正常終了しておらず、後続のアダプタ別試験にも到達していません。「UE外では完全に正常」「ドライバ全般の不具合を否定」は過大な結論です。生成成功の観測と、その先の未検証を分け、プローブのCOM定義/ABIを点検してください。プローブ内の例外をGPU障害と解釈することもできません。
- R2：最小プロジェクトの証拠ログは `Running DelayedAutoRegister Phase PreRHIInit` の後に約60秒で終了しており、実プロジェクトの `Checking if RHI D3D12 ...` はありません。同じ停止APIと断定できません。「複数プロジェクトで起動失敗する」証拠はありますが、全ケースの停止位置が同じという記載は修正してください。
- R3：スタック取得で必ず原因/APIを確定できるとは限りません。シンボル不足や待機先の別スレッド/ドライバの情報不足があり得ます。実プロジェクトのIsSupported内という説明もログとソースからの推定で、実スタックによる確認ではありません。
- R4：再現・復旧節の一括「Savedへコピー」は不正確です。walkthrough-verification.jsonとwalkthrough-smoke.logはプロジェクト直下、smoke-state.json/txtはSaved配下です。元パスとの対応を明記してください。

## 推奨する次の調査（実行はClaude Code）

選択肢1を推奨します。Engine/ドライバの変更はまだ行いません。まず既存のデバッガとUE版に一致するPDBの有無を確認します。必要ならMicrosoft公式ProcDumpをローカル展開し、解析用WinDbg/CDBも確保します。ProcDumpは採取用であり、それだけでスタック解析が完了するものではありません。

同じ実プロジェクトの失敗を再現し、RHI確認ログ後の待機中に対象PIDを固定して、5〜10秒間隔で2〜3個のユーザーモードダンプを取得します。全スレッドのスタックとロード済みモジュール/版を比較し、同じ待機が続く箇所と呼出し元を報告します。取得時の一時停止がタイミングへ影響し得ることも記録します。終了後のダンプだけでハング時の状態を代用しません。

常駐デバッガ登録やクラッシュハンドラ登録（ProcDump -i）、ドライバ入替、Agility SDKのリネームは今回の採取に含めません。ダンプはプロセスメモリを含むためbuild配下のローカル資料とし、Gitへ追加しません。採取コマンド、時刻、対象PID、コード/Engine版、シンボル読込状況、匿名化したスタック要約、ダンプのハッシュを次の報告に含めます。

スタックが読めなければ「原因不明」のまま不足しているシンボル等を報告します。ドライバ/SDKの変更は、その証拠を見て次に判断します。調査方針への推奨であり、このレビュー自体で環境変更を実行してはいません。

参考：[Microsoft ProcDump](https://learn.microsoft.com/en-us/sysinternals/downloads/procdump)
