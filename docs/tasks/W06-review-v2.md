# W06 再レビュー v2

判定：CHANGES_REQUESTED。2026-09-09。
BASE：d4fabb229a2b53e2478486e3913f4dd4563fbbbf
対象HEAD：1962388fe8a499fda76149738e430e61b569edcd
レビュー束：build/reviews/W06-v2

## 結論

R1〜R3は今回受け入れます。R4/R5は大半が修正されていますが、下記2点だけ同じW06で継続修正してください。新しい機能・造形改善・全経路の検証は求めません。W07は未着手を維持します。

| 前回項目 | 今回の判断 |
|---|---|
| R1 取付け高さ | ゲストLDKのWeb高さ計算・入力逆変換を勾配天井へ接続。Pythonも室のピース欠落を区別。受入 |
| R2 発光方向・器具 | source下向きの修正、Blenderの余分な反転削除、emitPositionM、器具上下端、UE二重メッシュ削除を確認。受入 |
| R3 Blender夜間 | SUN energy/World Strengthのnight時0、day時の既存値維持をコードと提出画像で確認。受入 |
| R4 参照検証 | 状態の未知器具IDを生成/案保存・適用前に拒否、プロファイル/グループ差分表示は受入。グループ参照切れの停止が未完 |
| R5 比較 | solar保持・比較開始の相互排他・照明編集拒否・実効値表示は受入。照明以外の通常編集/案読込に抜け |

## 必須修正A：グループの参照切れも適用前に拒否する（R4継続・中）

対象：scripts/build-visual-twin.py:119付近、scripts/refresh_inputs.py:38、blender/electrical_assets.py:121、unreal/study_controls.py:975。

共通解決build_lighting_bindings()はgroupsを検証しません。レビューで実データのgroup.fixtureIdsへdeleted-fixtureを1件追加して呼び出したところ、正常に3灯を返しました。生成前チェック/案保存へもこのグループ不整合検出は接続されていません。UEでは警告ログを出した後、既知メンバーだけへ操作を継続します。

正本から器具を削除してグループへの参照が残ることは通常の編集で起こります。「グループ全体を点灯」のつもりで一部だけが点灯する状態を、ログだけで通さないでください。前回R4の要求は事前確認と拒否であり、警告による部分適用への仕様変更は承認していません。

共通検証で照明設定の構造とグループの器具参照を確認し、生成/refresh/案保存の入口へ接続してください。UE側でも不明メンバーがあれば、現在状態を保持したままグループID・器具ID・修正先を案内して拒否します。既に正常な器具上書きの経路を再設計する必要はありません。

確認は不明メンバー1件を含むグループで、生成前拒否と操作時の非部分適用を確かめるだけで十分です。

## 必須修正B：比較中の固定条件変更・案読込にも同じガードを適用する（R5継続・中）

対象：unreal/study_controls.py:208（set_variant）、212（set_elevation）、230（set_sun_case）、面編集関数、602（load_scenario）等。

_require_no_active_compare()は照明編集と比較開始には入りましたが、既存の仕上げ変更・太陽変更・案読込には入っていません。レビューでソースからset_variant()を抽出し、current_state/apply_stateのみを簡易代替して実行すると、_lighting_compareが有効なままvariantがnaturalからwarmへ変わりました。これは実UEの再現ではなく、該当製品関数のガード欠落を確かめた軽量確認です。

照明A表示→仕上げ変更→B表示と操作すると、Aの固定条件が途中だけ変わり、変更は次の比較表示で消えます。案読込はsite.local.jsonとsun-cases.jsonも書き換えるため、比較開始前の状態との組合せも崩れます。

比較で固定する条件を変える通常UI操作（仕上げ・面編集・太陽条件・比較用視点の更新・状態復帰・案/敷地読込）へ既存のガードを入口で適用してください。内部apply_state()そのものを一律拒否するとA/B表示や終了も壊れるので、外部の通常操作だけに適用します。参照・一覧・選択・保存など、固定条件を変更しない操作まで禁止する必要はありません。

確認は照明比較中に仕上げ変更と案読込が拒否され、比較終了後は通る代表操作で十分です。他比較の全組合せ・全ボタンの実機網羅は不要です。

## 確認内容・限界

- 追加差分18ファイルと関係する呼出し先を確認。提出証拠9件のSHA-256は全件一致です。
- test_electrical_assets、test_refresh_study、test_study_scenarios、test_source_changesをレビュー側で実行し、いずれも成功しました。
- Blender夜間の提出画像を目視。UEのダウンライト方向[ほぼ0,0,-1]、二重メッシュなし、solar保持、照明編集拒否、完全refreshのcompleteを提出JSON/スクリプトで確認しました。
- レビュー側では重い再生成/UE起動をしていません。前回のスモークは同一プロセス内の保存復帰を含みますが、報告の「別プロセス再起動まで同じ証拠で確認済み」は区別してください。今回は追加実行を受入条件にせず、報告を実際の証拠範囲に合わせれば十分です。
- 実品番/配光/照度未校正、間接照明/外灯、全館化は従来通り対象外です。壁付け光源の本体との位置関係は実配置時の確認事項として残します。これを理由に今回の追加実機検証は要求しません。

## 再提出

元BASEを維持し、上記A/Bだけまとめて修正、W06-report.mdを更新してbuild/reviews/W06-v3を作成してください。今回は光源生成・材質・C++を変更しない限りBlender/UEの完全refresh再実行は不要です。少数の関数テストと既存UEでの代表操作で十分です。Sonnetで継続できます。
