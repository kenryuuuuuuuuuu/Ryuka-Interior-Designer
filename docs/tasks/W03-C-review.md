# W03-C レビュー

判定：ACCEPTED（下記の非阻害残件をW04へ引継ぎ）
対象BASE：`55e9ab52e2456f335ab0909cd4d53c938ddb1f9f`
対象HEAD：`806c71c158de3dc1f89d3b79de982f1881d0eee9`
レビュー束：`build/reviews/W03-C-v1`

## 確認結果

面IDの登録・現在形状への解決・Blender前の停止を受け入れます。部屋側ごとのID、辺の順序変更への耐性、壁移動/分割での未解決と明示更新、床/天井の部屋形状への追従は実装と関連テストで確認しました。

- surface_registryの11件、build_visual_twin_surfacesの3件、refresh_studyの8件をunittestで再実行し、計22件成功しました。
- 提出差分のSHA-256と証拠4件のSHA-256はmanifestと一致しました。実データ解決結果は8面、issuesなしでした。
- パッケージ内の登録JSONは確認済みです。ただし検証モジュール自体の収集には下記の漏れがあります。
- 全82件・house検証・web-dataチェックの成功は実装報告によります。レビューでは関連テストのみ再実行し、Blender/UEの再生成は行っていません。

## 非阻害残件：W04の冒頭に含めること

1. `scripts/build-visual-twin.py`のinputs()へ `scripts/surface_registry.py` を追加してください。data以下のJSONは自動収集されますが、Pythonモジュールは明示リストであり、このファイルは提出パッケージのsourceHashesにもinputs/scriptsにもありません。現在のリポジトリからの通常生成は動作するため、W03-Cの再提出は不要です。W04ではinputs()に含まれることを軽い確認で確かめれば十分です。
2. `docs/ARCHITECTURE.md`の「現在の登録欠落は移行上の警告のみ」は誤記です。現在の登録欠落は実装通りエラー、過去のパッケージに登録がないことは許容、と修正してください。

## 次の作業

W04を原則1つの仕様・実装・レビュー単位で進めます。面別仕上げの実メッシュへの対応、操作、案の保存/比較、再生成後の復元をまとめて詳細設計します。上記2件をその指示に含めてください。

現在は識別・検証の基盤が完成した段階です。実際の塗装面の存在、共有壁の片面だけの材質適用、UEの面選択は未実装であり、W04で扱います。
