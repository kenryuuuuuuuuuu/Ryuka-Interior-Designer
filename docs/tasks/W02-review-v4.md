# W02 第4版レビュー

判定：CHANGES_REQUESTED（前回A/Bは対応確認済み。新設テストの隔離のみ残件）
対象HEAD：26a7af378e6cfb57052efcdafc9f9b6a9cce56b6
BASE：0ebb9f616930fa06436fddaed652e00a3334746c
資料：build/reviews/W02-v4

累積差分は前回確認済み部分と今回の追加差分で照合しました。patchと証拠11点のサイズ/SHA-256は全て一致。保存障害・再起動認識・解除後復旧・境界停止/復帰のJSON結果を確認し、48件のPythonテストを再実行して成功しました。UEの実入力と障害注入はレビュアーでは再実行していません。

## 合格とする部分

前回A：RecoverSaveIfNeededのディスク状態に基づく復旧、保存拒否、F9での停止、障害解除後の復元を確認しました。前回B：補正不能時のStopMovementImmediately/DisableMovement/bReady=false、および安全点を確保したF9での再開を確認しました。これらの製品ロジックの再設計は求めません。狭所の滑らかさも今回の追加作業には含めません。

## 残る必須修正 / 高：障害注入テストが通常保存を削除する

対象：Walkthrough.cppのSavedViewNameとTick内RyukaFaultSaveブロック。

SavedViewNameはRyukaSmoke以外で通常のSaved/walkthrough-state.jsonを返します。RyukaFaultSaveはそのPathとBackupに対してDeleteおよび再帰DeleteDirectoryを実行してからテストを始めます。そのため、通常の保存があるプロジェクトでこのスイッチを指定すると施主の保存とバックアップを消してしまいます。今回の実施場所が専用生成プロジェクトだったことは、コードの誤実行防止にはなりません。また障害注入行だけが!UE_BUILD_SHIPPINGで囲まれており、Tickの削除処理を含むテストブロック自体はそのガードの外にあります。

### 最小の修正方針（今回の範囲はこれだけ）

1. FaultSave/VerifyRecovery/BoundaryFaultTestは共通のテスト専用保存名（例：Saved/walkthrough-test-state.jsonと.bak）を選択し、通常の保存には読み書き・削除を行わないようにします。VerifyRecoveryも同じ名前を使い、別プロセスでも同じ復旧判定を確認します。既存smokeの専用保存は維持します。通常起動の名前・形式は変えません。
2. 破壊的なテスト本体を含む全テストブロックを!UE_BUILD_SHIPPINGへ揃えます。通常の起動でテストフラグがなければ製品の挙動はそのままです。
3. 削除は専用の既知ファイルだけに限定します。障害注入用のディレクトリは自分で作った空ディレクトリだけを非再帰で削除し、想定外の中身があれば停止します。通常保存のディレクトリ名を再帰削除してはいけません。
4. 確認：通常保存と通常Backupに異なる識別用内容を入れた専用プロジェクトで、テスト一式を実行し、両方の前後ハッシュが一致することを証拠にします。再起動・障害解除のテスト専用保存での成功も維持します。製品の保存処理を別実装へコピーせず、同じSaveView/RestoreViewを保存先だけ変えて検証してください。

新しい汎用テスト基盤・追加機能は不要です。コード計装と再現テスト追加の許可は継続しています。通常保存を守る確認用の入力データも施主の実ファイルではなく検証用に作成します。

## 証拠の小さな整理

manifestの複数logが同一ハッシュです。ランチャー出力が同じこと自体は不具合ではありませんが、各ケースの結果は個別のネイティブJSON/txtも直接--artifactで収録してください。decoded.jsonを用いる場合は元ファイルと変換手順を対応付けます。

## 次の提出

Sonnetで上記の隔離修正に限定して対応し、同じBASEからbuild/reviews/W02-v5を作成してください。今回対応済みのA/Bや以前の画角・HUDを再実装する必要はありません。新しい内覧機能の仕様は追加しません。テスト隔離・通常保存保護と既存検証を確認できればW02の最終受入を判断します。
