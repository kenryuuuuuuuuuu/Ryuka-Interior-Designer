# W08-G レビュー v1

判定：**CHANGES_REQUESTED**（2026-09-11）。通常の保存・取込・GUI操作に関わる下記3点を一括修正してください。W07-H1以降へは進みません。

- BASE: `1d6a11bb4f035b4aabaf9209766f54c677f18cc3`
- HEAD: `d6f17d8ab681350a9d606dd3cc5107eaed1ebcf8`
- 提出束: `build/reviews/W08-G-v1`

## R1 — P2: 比較撮影が最新のF5保存を使わず、太陽高度も無断で45度へ変えます

対象：`scripts/guest_launcher/comparison.py:54–89`、`unreal/capture_study.py:19–39`、`unreal/study_controls.py:54–61`。

ランチャーの比較記録はstudy-state.jsonの存在だけを確認し、capture CLIへvariantとelevation=45を渡します。撮影側のcurrent_state→initial_stateはエディタのstudy-state.jsonを読み、最新のSaved/walkthrough-state.jsonを選択しません。内覧で別室へ移動・点灯・開扉・F5保存して終了しても、比較記録は古い部屋・視点・灯り・扉で撮影されます。案保存が共通の最新保存選択を使うのに対し、同じ入口の撮影は別の状態になります。

さらにGUIは「対象室の仕上げ違いを固定カメラで撮影」としか示さず、撮影時に保存した太陽高度を45度へ変え、solar来歴も削除します。手動角度の保存であっても45度へ変更する理由にはなりません。

共通の最新有効保存選択と検証を使い、撮影開始時に選んだ状態をA/Bで固定してください。元の保存を上書きせず、対象室variant以外の視点・他室・fixtures・扉・太陽/来歴を保持します。明示的な撮影条件変更を提供する場合のみ、GUIで値と変更内容を確認できるようにしてください。

確認：エディタ保存とF5保存を異なる室/視点/点灯/太陽にし、新しいF5保存からA/Bを1組記録。両画像の条件が選択した保存と一致し、変更が対象室の仕上げに限定されること。元の保存ファイルが不変であること。

## R2 — P1: 差分確認後に変更された正本を、確認なしで上書きします

対象：`scripts/guest_launcher/app.py:261–267`、`scripts/guest_launcher/furniture.py:202–229`。

表示したCandidateReportには正本/候補のハッシュや確定内容がなく、反映時は候補のパスだけを渡します。apply_candidateは新しい正本に対して再検証しますが、変わった差分を利用者へ再表示せずそのまま全件置換します。差分画面を開いたあと別の取込画面や編集で正本を更新した場合、新しい変更が消えます。候補ファイルを同じパスへ再書出しした場合も、見ていない内容が反映されます。

隔離したbuild内のコピーで、候補Aの差分表示→正本の別家具にnote追加→apply_candidate(A)を実行したところ、反映は成功扱いで追加noteが消えました。Web生成のsubprocessだけをスタブ化しており、レビュー中に実際のdata/furniture.jsonは変更していません。

プレビュー時の正本と候補の内容を確定し、反映時の一致を確認してください。どちらかが変わった場合は書込み前に止め、再比較・再確認へ戻します。バックアップがあることは、未確認変更を上書きしてよい理由にはなりません。

確認：通常反映のほか、プレビュー後の正本変更1件と候補変更1件を軽量確認し、どちらも未確認の内容を書き込まないこと。

## R3 — P1: 更新画面を閉じると、GUIの完了処理と状態切替が停止します

対象：`scripts/guest_launcher/app.py:83–98,327,339–380`。

更新実行中も「閉じる」/ウィンドウの×はUpdateWindowをdestroyします。一方、実行スレッドからのログと完了コールバックは破棄されたsteps_box/run_btn等を操作し続けます。_drain_queueはqueue.Emptyしか捕捉しないため、破棄済みウィジェットのTclErrorが出ると最後のafter登録に到達せず、以後のログ・完了処理が止まります。refreshが成功しても現行モデルの切替に到達できなくなります。

実際の_drain_queueに、破棄済みウィジェットが出すTclErrorを模したコールバックを入れた軽量再現で、例外伝播・再スケジュールなし・後続コールバック残留を確認しました。実GUIでのクリック再現ではありません。更新中の画面破棄が許可され、そこへ継続して書き込むことはコードで確認しています。

ジョブの完了・設定保存をウィンドウの寿命から分離するか、実行中の破棄を防ぐ等で通常の「閉じる」を安全にしてください。キューは一つの表示先の消失で全体停止しないようにします。アプリ本体終了時も、実行中ジョブを無案内で孤立させず、終了を止めるか再起動後に実行記録を照合できるようにしてください。強制終了や高度なキャンセル機能は不要です。

確認：短い疑似更新を使ってGUIから開始→更新画面を閉じる操作→成功/失敗表示と現行先の扱いを確認し、その後も別の操作の完了通知が届くこと。CLIだけの検証ではこの経路を通りません。

## 確認済み・証跡の限界

- レビュー側のunittest discoverは **217件成功**。pytestの再実行ではありません。
- manifestの成果物 **14点すべてSHA-256一致**。提出LDK natural/warm画像は実際に仕上げが異なり、captureの複数室variant修正は反映されています。ただし最新F5の状態選択の確認にはなりません。
- 更新v2のrefreshはcomplete、取込/転送等の成功記録を確認しました。sourceHashesと現在コードの差はunreal/capture_study.pyです。撮影CLIがこのファイルをコピーする経路も確認しました。
- 更新v2の実保存状態は全8室natural、fixtures空、doorStates={}、カメラ非nullです。W08-G AC4の「新旧室で異なる仕上げ/点灯・開扉を持つ保存からランチャー更新」は、この提出結果では未確認です。次の代表更新1回には実際にその条件を含め、報告へ正確に記載してください。
- GUIは提出報告でもウィンドウ構築確認までで、通常操作一巡は補助CLIが中心です。今回レビューでもUE/Blenderの重い工程やGUIの実操作は再実行していません。コード追跡・軽量再現と提出証拠を区別して判定しています。

文書訂正：GUEST_TRIAL_GUIDE.mdの「玄関等は編集対象外」はpilot時代の記述で、guestは8室すべて編集対象です。報告のBASE内の空白とHEADも提出manifestに合わせて訂正してください。

## 再提出

元BASEを維持し、R1〜R3を修正して `docs/tasks/W08-G-report.md` と `build/reviews/W08-G-v2` を提出してください。関連テストに加え、GUIの起動・取込・更新・保存案/記録への代表操作を確認します。R2/R3は軽量確認でよく、重い検証は最終コードの代表更新1回とA/B1組へまとめます。G2全経路や全室画像の再網羅は求めません。
