# W07-G2 最終レビュー v4

判定：**ACCEPTED**。2026-09-10。

- BASE：`60d8cd644d00e57f11708dbbb1a1aa263095b2bc`
- HEAD：`e9113b94d03f6eaf5c83bede32fc9532bca470c0`
- 提出：`build/reviews/W07-G2-v4`
- 前回：[再レビューv3](W07-G2-review-v3.md)
- 作業場所：`build/worktrees/visual-twin` / `feature/visual-twin-foundation`

## 結論

残っていた「内覧の部分開放と保存・表示条件の不一致」は解消しています。G2を受け入れます。G1で作った室別状態に加え、ゲストの室間移動・建具開閉・保存復帰が今回の到達点です。G3の全8区画の仕上げ・主要設備・照明展開は未実装です。

## 前回R1の確認

固定壁に対する開角をcirculation.pyで生成時に解決し、Blender・UEインポート・エディタ・C++が同じopenYawDeltaDegを参照します。C++の実行時部分開放・再補間は撤去されました。葉が動かない同一状態の再適用では動作検査を行わず、実際の開閉で家具・他の葉・対話操作時の施主が干渉すれば全体を拒否します。

代表door-002について、以下の一致を確認しました。

| 証拠 | 結果 |
|---|---|
| 生成/エディタ側door-bindings | 73度 |
| 内覧の実測ログ | 72.99999999999994度（浮動小数の誤差内で73度） |
| refresh後のBlender/UE bindings | ともに73度、bakedOpen=true |
| refresh後のinterior.glbの実ノード | 四元数の回転は約73度（座標系変換に伴う符号反転あり）。開ポーズが実際に書き出されています。 |
| エディタ実行結果 | 実葉角度、太陽変更中、比較A/B/終了、レベル保存・再読込後の角度保持が成功 |

旧85度へ戻す経路と、現在の部分開角から再計算する経路は今回の修正で除かれています。保存のopen=trueが、全経路で同じ生成条件の開ポーズを指します。

## 検証と受入条件

- v3→v4の8ファイルの変更一覧・差分と直接の影響先、累積変更一覧を確認。manifestの9成果物は全てSHA-256一致です。Blender成果物の権限制限は読取のみの昇格で解消しました。
- refreshは全7工程complete。主要4ファイルの実行時ハッシュは最終コードとLF正規化後に一致。今回は非null cameraと開扉状態を入力にし、内覧構築・状態転送検証まで成功しています。
- レビュー側でunittest **192件成功**。house validator、生成データ--checkも成功しました。
- 実UEの連続経路ログには失敗のboolean項目がなく、閉動作中の施主干渉拒否、離れた後の閉扉、洋室保存復帰、代表扉の通行が記録されています。エディタ検証は実葉とJSONを分けて確認しています。
- AC1の接続、AC2/3の代表通行・開閉、AC4の洋室保存復帰は前段の修正と今回の提出結果を合わせて受入。AC5/6の比較・旧案・refreshは今回の実開ポーズ統一により受入。AC7の既存検証と文書も確認済みです。
- UE/Blenderをレビュー側で再起動して全経路を追試したものではありません。実機動作は提出スクリプト・結果・ログ、生成形状はGLB読取、変更契約はコードから確認しています。

## 継続する制約

安全角は現行ゲスト形状に対する2Dの概算（壁厚・余裕を含む）です。実物の建具金物の開角や全館の通行安全を認定するものではありません。別の部屋/複雑な壁/建具型へ広げる際は再解決と代表確認が必要です。今回の対象外へ一般化する追加実装は要求しません。

任意の手動エディタ視点remember_viewの室帰属はactiveRoomId基準という既知の制限を維持します。選択室の初期視点と内覧F5/F9の確認済み経路とは区別し、今後自由視点操作を仕上げる際に実座標での帰属解決を見直します。G2の受入を止める条件にはしません。

## 次の段階

G2の追加修正・再提出は不要です。次は、受入済み実装に合わせてG3の詳細仕様を確定します。レビュー合格による自動push/mergeやG3製品実装は行っていません。再起動後の再開時は、本書をG2の最新判定として参照してください。
