# 背景・経緯（なぜこうなっているか）

このドキュメントは、プロジェクトの目的・データの作り方・過去に見つけた落とし穴を記録する。**新しくこのリポジトリを開くエージェントは、作業を始める前にこのファイルを読むこと。** 技術的な仕組み（正本・変更手順）は [ARCHITECTURE.md](ARCHITECTURE.md)、現在の進捗・未解決事項は [STATUS.md](STATUS.md) を参照。

## 1. このプロジェクトの目的（なぜこれを作っているか）

施主が2027年4月の竣工を予定している新築住宅（主居＋民泊棟の二世帯構成）の内装を、着工前に検証・意思決定するためのデジタルツイン。最終的な狙いは3つ。

1. **寸法・間取りの精度を上げること。** 施工会社の実施図面はCAD原本ではなくスキャンPDF/画像しかない。断面図もない（天井高が図面から読み取れない）。この白模型は、複数の参考資料を突き合わせて壁位置の矛盾を洗い出す検証ツールとして機能している
2. **施工会社への要望を視覚的に伝えること。** 平面図の口頭説明より、3Dモデルの方が伝わる
3. **将来的な意思決定の土台。** 家具配置・コンセント/照明位置・内装の質感検討を、この骨格の上に積み上げていく

**Three.js vs Blenderの位置づけ**：Three.jsは「寸法検証・骨格確認」に強く、実際にその通り機能した。一方「本物のような質感」（施主が当初イメージしていたゴール）は、Blenderの方が労力対効果で有利という評価を、2026-08-13のセッション中に伝えている。現時点でどちらに倒すかは未確定（[STATUS.md](STATUS.md) の未解決事項を参照）。

## 2. データの作り方（なぜこの数値なのか）

**ここが一番失われやすい文脈なので、必ず読むこと。**

### 2.1 情報源の優先順位

1. 施工会社の実施図面（Ver5、配置図・求積図・平面図・立面図の4葉。断面図はない）
2. `Ryuka-Landscape-Designer`リポジトリ（外観モデル、外皮の階高・軒高が実施図面と検算済みで確定）
3. マイホームクラウド（間取り作成サービス）の間取り画像。**面積・寸法線は信頼できるが、天井高・床高は物理的に矛盾する値を含んでおり、一切使用していない**
4. 上記から読み取れない部分は、910mmグリッド（尺モジュール）を前提に、面積からの逆算や画像のピクセル解析で補完

**PDF原本の所在**：上記1の実施図面PDF（`渡辺拳竜様邸新築工事.pdf`、Ver5・全4葉）は`docs/`に置いてあるが、施主の氏名・住所・電話番号を含むため`.gitignore`で除外しており、GitHub上には存在しない。ローカルのファイルシステムには残っているので、新しいエージェントは`docs/`配下を直接確認すること。この環境には`pdftoppm`（poppler-utils）が入っていないため、`pip install pymupdf`でPDFページをPNG化して読む方式を使った。

### 2.2 手法：画像のピクセル解析

CAD原本がないため、施主がマイホームクラウドの画面やこの白模型のスクリーンショットに赤線・青線を描き込み、それを都度ピクセル単位で解析して実寸に変換する、という作業を数十回繰り返して座標を確定した。

**技術的な教訓**：

- **キャリブレーションは離れた2点を基準にする。** 近接した2点（間隔0.9m程度）で比率を出すと、数ピクセルの誤差が大きく増幅される。過去に一度、この理由で壁位置を誤って「訂正」し、さらにそれを再訂正するという手戻りが発生した
- **赤/マゼンタ/水色などの色は、既存の描画要素（ドア=マゼンタ、窓=水色）と被ると誤認識しやすい。** ユーザーの手描き線は「純色（例: R<80,G<130,B>180）」、モデルの描画要素は半透明のブレンド色になることが多く、RGB値で判別可能
- **求積図の外形区分（A1〜A4ゾーン）と、実際の部屋割りは一致しない。** 例えば「LDK」は外形ゾーンをまたいで存在し、北側の壁が3段階（z=2.730→2.275→1.820）の階段状になっている実例がある

### 2.3 確度（status / conf）の運用

`高`/`中`/`低`（JSON側では`verified`/`derived`/`estimated`）の3段階で運用している。定義は[README.md](../README.md)の「データの確度について」の表を参照。

## 3. 見つかった不具合と教訓（同じ轍を踏まないために）

2026-08-13のセッション中に発見・修正したバグを列挙する。多くはThree.js特有の癖なので、Web表示ロジックを触る際は要注意。

| 症状 | 原因 | 対処 |
|---|---|---|
| 一部の部屋だけ色が薄い/白く見える | 半透明オブジェクトの`depthWrite`がデフォルトで有効なため、重なった別の半透明レイヤーが下の色を隠す | 全ての半透明マテリアルに`depthWrite:false`を設定 |
| 部屋の輪郭線がチラつく/薄い | 隣接する2部屋が同じ境界線を別々に描画し、Z-fighting | 輪郭線マテリアルに`depthTest:false`を追加し、`renderOrder`だけで描画順を決定させる |
| 矩形とL字ポリゴンで色味が違って見える | `MeshStandardMaterial`は光源方向・法線で陰影が変わる。ポリゴン(ExtrudeGeometry)と矩形(BoxGeometry)で法線の向きが揃わないと陰影差が出る | `MeshBasicMaterial`（ライティング非依存）に変更 |
| 2F西面の窓がx=0（1F西端）に誤配置 | `face:'W'`の開口部は常に絶対西端(x=0)に固定する実装だった。2F自体の西壁はx=12.740 | 開口部データに`x`フィールドを追加し明示指定できるよう改修 |
| ドアマーカーが壁からわずかにはみ出す | 部屋の境界ぎりぎりにドアを配置すると、数cmの計算誤差で越境する | ドアは壁の両端から余裕（0.05m以上）を持たせて配置。`tests/validate_house.py`に「全ドアが対応する壁の範囲内に収まっているか」の機械チェックを追加済み |
| 2つの窓が繋がって1本の長い窓に見える | 同じ壁(x=12.740)上に配置した2つの窓のz範囲が一部重複していた | 個別の窓のz範囲が重ならないことを確認してから配置する |
| rooms→walls変換で、複数ゾーンにまたがる壁が外壁として除外されない | 「壁の区間全体が1つのfootprintに収まるか」で判定すると、2つの隣接footprintにまたがる外壁を見逃す | 区間の「引き算」方式（該当座標の外皮区間を全て集めてから差し引く）に変更 |
| `house.json`の1F/2F床レベル(fl1/fl2)が図面と207mm矛盾 | 基礎高(GL+500mm)と1階FL(GL+707mm)を取り違えていた。fl2もfl1+階高で計算していたため同じ誤差を引き継いでいた | 東側立面図の実測値(1階FL=GL+707mm、1階階高2,732mm)に合わせて修正（2026-08-14、詳細は4章） |
| 片流れ屋根レイヤーをオンにすると、屋根が間取りの下に隠れて見える | 全ての半透明マテリアルで`depthWrite:false`にしているため、前後関係は実際のY座標ではなく`renderOrder`の数値だけで決まる。屋根メッシュ(`slopedRoofBox`)だけ`renderOrder`を設定し忘れていて既定値0のまま描画されていた（外皮=1、室内間仕切り=2、ドア/開口部=3より低い） | `slopedRoofBox`に`renderOrder`引数を追加し、屋根を4（他レイヤーより後＝手前）に設定（2026-08-14） |
| `Ryuka-Landscape-Designer`の`data/fixed-site-data.js`にも同じfl1/fl2の誤りがある | 同リポジトリのコメントに「基礎高0.500」と明記されており、`fl1`という変数名でありながら実際は基礎高を表す値として設計されていたと判明。当リポジトリのHTMLは元々ここからLEVELSをコピーしていたため、誤りごと引き継いでいた | 当リポジトリでは4章の通り立面図の実測値に修正済み。`Ryuka-Landscape-Designer`側は未修正（別リポジトリのため対象外）。今後同リポジトリの値を参照する際はfl1/fl2が基礎高基準である点に注意 |
| `node scripts/build-web-data.mjs --check`が、実際にはgenerated/house-data.jsが最新でもSTALEと誤判定することがある | WindowsのGit（`core.autocrlf`）が`.js`ファイルをチェックアウト時にCRLFへ変換するが、Node.jsの`fs.writeFileSync`はLFで書き出す。ファイル内容は実質同じでも、生の文字列比較では改行コードの違いだけで不一致になる | 比較前に`\r\n`を`\n`へ正規化するよう`--check`のロジックを修正（2026-08-15） |
| 表示フロアを1F/2Fに限定していても、非表示側の家具がクリック選択されてしまう | Three.js(r128)の`Raycaster.intersectObject`は`Object3D.visible:false`のオブジェクトを自動的には除外しない。`groups.furniture2`を非表示にしていても、レイキャストは素通りしてヒットする | ヒットしたオブジェクトから祖先を辿り、`visible:false`のノードがあれば除外する`isEffectivelyVisible()`を追加（2026-08-15） |

## 4. 作業上の約束事

- コミュニケーションは「ですます調」。個人名は使わず「施主」、施工会社名も「施工会社」と汎用化する（このリポジトリはGitHub無料枠のためPublic。個人情報を含めないこと）
- 保守的な数字を優先し、楽観的な仮定は`estimated`と明示する
- 変更は`git checkout -b`でブランチを切り、PRを作成する。施主が承認・マージする運用
- 建物データの変更手順は[ARCHITECTURE.md](ARCHITECTURE.md)の「変更手順」に従う（house.json編集→validate_house.py→build-web-data.mjs→ブラウザ確認→必要ならBlender再生成）
- データを移動・変換する際は、根拠・確度・検証メモ（`note`フィールド）を必ず引き継ぐ。捨てない

## 5. 変更履歴ログ

### 2026-08-13：Claude.aiセッション

間取り・ドア・窓の座標確定〜rooms/walls抽出（Phase 1b、変換スクリプトはリポジトリ外）。詳細は2〜3章。

### 2026-08-14：Claude Codeセッション（施工会社図面PDF精読・データ正本の反転）

- 施工会社図面PDF（`docs/渡辺拳竜様邸新築工事.pdf`、Ver5）を`pymupdf`でページ画像化し精読した
- **天井高2,400mm推測値の傍証**：断面図はないが、東側立面図に階高の実測値が記載されていた。1階階高（1F床〜2F床）2,732mm、2階階高（2F床〜2階軒高）2,861mm、1階FLはGL+707mm。階高2,732mmから天井高2,400mmを引くと床・天井下地厚として332mm残り、一般的な木造の下地厚（概ね300〜350mm）と矛盾しない。ただし天井下地の実寸は依然不明のため、`status`は`estimated`のまま据え置いている
- **1F/2F床レベルの誤りを修正**：`house.json`の`levels.fl1/fl2`が基礎高(GL+500mm)と1階FL(GL+707mm)を取り違えていたと判明。立面図の実測値に合わせて`fl1:0.707`、`fl2:3.439`に修正（3章の表を参照）。`eaveLow`/`eave2`/`ridge`は別の寸法チェーン（基礎高基準）から取っており、修正不要だった
- **「小屋裏まで張り上げ」壁の注記**：1階平面図に「斜線部の壁はPB t=12.5 小屋裏まで張り上げとする」という注記があり、引き出し線を追跡すると`house.json`の`sound-wall-01`（民泊-自宅防音壁、x=7.28）と同じ位置を指していた。この平面図上でこの注記が指す位置は防音壁1箇所のみに見えるが、平面図全体を網羅的に確認したわけではないため中程度の確度
- **データ正本の反転**：それまで`interior-white-model.html`が一次資料で`house.json`はそこから同期される構成だったが、逆転させた。`house.json`を唯一の正本とし、`scripts/build-web-data.mjs`が`generated/house-data.js`を生成、HTMLはそれを`<script src>`で読み込むだけになった（`scripts/sync-house-from-html.mjs`は削除）。詳細は[ARCHITECTURE.md](ARCHITECTURE.md)
- **各エンティティにnoteフィールドを追加**：HTML側にあった訂正履歴コメント（例：「訂正(2026-08-13)：施主指摘の青線位置に合わせ左へ移動」）を、`rooms`/`openings`/`specialWalls`の`note`フィールドへ移植した。正本がJSONになったことで根拠コメントが失われないようにするため
- **ドキュメント再編**：`PHASE-1.md`と`WORKFLOW.md`を`ARCHITECTURE.md`に統合、`HANDOFF.md`を本ファイル（`BACKGROUND.md`）に改名、進捗・未解決事項を`STATUS.md`に切り出した。すべて日本語化した
- **参考リポジトリ**：`https://github.com/kenryuuuuuuuuuu/Ryuka-Landscape-Designer`（外観・畑の範囲シミュレーション）。外皮の階高・軒高の検算元として2.1節で言及されている
- **屋根のrenderOrder不具合を修正**：3章参照。片流れ屋根が室内間仕切りより奥に描画されていた
- **「外皮・ゾーン壁」表示レイヤーを削除**：施主指摘により、`FLOOR1`/`FLOOR2`（求積図のA1〜A4ゾーン境界）は実在する壁ではなく計算区分でしかないため、Three.js側の表示（チェックボックス・凡例・半透明の箱と輪郭線）を削除した。床スラブの表示とhouse.json側の`footprints`データ（Blenderの外壁生成・開口部の所属ゾーン判定に必要）はそのまま残している
- **1F/2F切替を「薄く表示」から「非表示」に変更**：`setFloorDim`によるopacity調整をやめ、`approx1/2`・`openings1/2`・`doors1/2`・`floor1/2`・防音壁・屋根の各グループを`visible`で直接切り替える方式にした
- **2階切妻屋根を追加**：`Ryuka-Landscape-Designer`の`js/building-model.js`・`docs/MODEL-SPEC.md`§3.4.2から3寸勾配・棟東西方向・軒出寸法（北南0.558m、東西破風側0.220m）を移植。`house.json`の`roofs`に`kind:"gable"`のエントリとして追加し、既存の片流れ屋根には`kind:"lean_to"`を明示した。東西の妻壁（屋根下を塞ぐ三角壁）と、`Ryuka-Landscape-Designer`が別途持つ南東部A4上の下屋（§3.4.3）は今回のスコープ外で未移植
- **GitHub Pages公開・PWA化**：`Ryuka-Landscape-Designer`と同じ構成（`index.html`のリダイレクト、`manifest.webmanifest`、`sw.js`、`icon.svg`）を移植し、`https://kenryuuuuuuuuuu.github.io/Ryuka-Interior-Designer/` で公開した。GitHub Pages自体の設定は`gh api`で有効化。オフライン起動のためThree.js本体を`vendor/three.min.js`にローカル同梱し、CDN参照をやめた。詳細は[ARCHITECTURE.md「公開」](ARCHITECTURE.md#公開github-pages--pwa)
- **一人称視点の内覧モードを追加**：`data/house.json`の`walls`（内壁）と`footprints`（外壁は「区間の引き算」方式で区画境界の相殺辺を除外して導出、rooms→walls変換時と同じロジック）からドア開口を差し引いた「壁セグメント」を共通データとして作り、(1)内覧モード用の実壁・天井メッシュ、(2)円と線分の距離判定による衝突判定（プレイヤー半径0.26m＋壁厚/2）の両方をそこから生成した。スポーン地点（民泊棟玄関／自宅玄関）は`openings`の玄関ドアデータから自動計算している。操作はデスクトップ・スマホともドラッグで視点操作、移動はWASD／矢印キー（デスクトップ）かバーチャルジョイスティック（タッチデバイス）。壁の当たり判定は実測寸法ではなく概算データからの導出のため、判定自体も概算

### 2026-08-15：家具・設備の配置（第1段階）

家具配置の実装方針について事前に相談し、以下5点で合意してから着手した。

1. 見た目は箱・円柱の組み合わせで作る。既製3Dモデル（GLB等）は使わない — サイズを自由に調整できること、PWAのオフライン保存が軽いことを優先
2. データは`data/furniture.json`に置き、`house.json`とは分ける
3. 編集は将来Web UI上のドラッグで行い、作業中はブラウザのlocalStorageに自動保存、決まったらJSONを書き出してGitにコミットする二段構え（`Ryuka-Landscape-Designer`がlocalStorageのみで機種間同期されない弱点を踏まえた設計。localStorage連携は未実装、現状は`furniture.json`を直接編集する運用）
4. 回転は0/90/180/270度の90度刻みに限定する（当たり判定の実装コストを抑えるため）
5. 「造作」（施工会社が設置し動かせない：キッチン・洗面台・UB・便器）と「置き家具」（後から自由に動かす：ベッド・ソファ・テーブル等）を区別する

実装内容：

- `data/furniture-catalog.json`（新設）：家具・設備の「型」ライブラリ。18種類（キッチン・洗面台・UB・便器・冷蔵庫・洗濯機・ベッド3種・ソファ2種・ダイニングテーブル2種・椅子・テレビボード・デスク・棚・ワードローブ）。寸法は日本の住宅設備・家具の一般的な標準値（施主の実測値ではない）
- `data/furniture.json`：33個の配置インスタンス。1F民泊棟・自宅LDK・水回り、2F各寝室など主要な部屋一通りに仮配置。すべて`status: estimated`
- `data/furniture.schema.json`（新設）：`furniture.json`のデータ契約
- `tests/validate_furniture.py`（新設）：カタログのtype参照・house.jsonのroom参照・rotation値などを検証。部屋の外形から大きく外れた配置がないかも機械チェックする
- `scripts/build-web-data.mjs`を拡張し、`FURNITURE_CATALOG`・`FURNITURE_ITEMS`を生成データに追加
- `interior-white-model.html`に`FURNITURE_SHAPES`（家具の種類ごとの組み立て関数）を実装し、家具・設備レイヤーとして表示。内覧モードでは概算の間仕切り等と違って**家具は表示したままにした**（「実際に通れるか」を確認する内覧モードの主目的に直結するため）
- Blenderへの家具生成は今回のスコープ外（施主の判断：配置の試行錯誤はインタラクティブ性が命なのでHTML側で詰める。決まった配置は将来Blenderからも読み込める設計にはしてある）
- 作業は`feature/furniture-placement`ブランチで実施

同じセッション内で、施主からのフィードバックを受けて以下も追加した（同ブランチ内の別コミット）。

- `cupboard`（カップボード）typeを追加し、自宅LDKに1台配置
- 便器をタンク有無で区別できるよう`toilet-tankless`typeを追加。1F自宅をタンクレスに変更、配置漏れだった1F民泊のトイレをタンク付きで新規配置
- 部屋ラベルと家具ラベルの表示ON/OFFを別チェックボックスに分離（それまでは家具ラベルが「部屋ラベル」と「家具・設備」レイヤーの両方に連動していた）

### 2026-08-15：家具・設備の配置（第2段階：Web UI編集）

`feature/furniture-placement`をmainへPRマージした後、`feature/furniture-editor`ブランチで着手。ブランチを切るかmainに直接コミットするかの判断基準を[AGENTS.md](../AGENTS.md)に明文化したのはこの直前で、今回はその基準に沿って「新しい編集UI（仕組みの変更）」としてブランチを切った。

- 平面図モードに「✎ 家具編集」トグルを追加。ONの間は家具のクリック/タップ選択→ドラッグ移動、選択中はパネルから90度回転・幅/奥行/高さの変更ができる
- 編集内容は`FURNITURE_ITEMS`（生成データ）を直接書き換えず、`furnitureEdits`という差分だけを保持し、`effectiveFurniture()`で重ねて描画する方式にした。差分はブラウザの`localStorage`（キー`ryuka-furniture-edits-v1`）に自動保存されるが、**これは正本ではなく作業中の下書き**という位置づけ（`Ryuka-Landscape-Designer`がlocalStorageのみで機種間同期されない弱点を踏まえた設計、という第1段階での方針通り）
- 「furniture.jsonを書き出す」ボタンで、編集を反映した完全なJSONをブラウザからダウンロードできる。これを`data/furniture.json`に上書きしてコミットするのが正本を更新する唯一の手段（ダウンロードするだけではリポジトリには反映されない）
- 移動はマウス/タッチ共通のポインターイベントで実装。正射投影カメラのレイと家具の床面（floor level）との交点を解析的に求めて建物ローカル座標に変換している
- 選択中の家具は`THREE.BoxHelper`で黄色い枠を表示。回転・移動はメッシュを直接動かす軽量な更新、幅/奥行/高さの変更は形状を作り直す必要があるためその家具だけ再構築する、という使い分けにした
- 副次的に見つけた不具合：`node scripts/build-web-data.mjs --check`が、生成物の内容が同じでも改行コード（LF/CRLF）の違いだけでSTALEと誤判定することがあった。Windows環境のGitがチェックアウト時にCRLFへ変換する一方、Node.jsは書き出し時にLFを使うため。比較前に正規化するよう修正（3章の表も参照）
